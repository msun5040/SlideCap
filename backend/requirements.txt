# Slide Organizer Backend Dependencies

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy>=2.0.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# SVS/OpenSlide support for slide viewing
openslide-python>=1.3.0
openslide-bin>=4.0.0

# Tile serving (imported unconditionally by main.py)
large-image[tiff]>=1.27.0

# TIFF tag parsing — pyramid conversion (tiff_pyramid) and the QC truncation check
tifffile>=2024.1.0

# JWT session tokens
pyjwt>=2.8.0

# Embeddings + dimensionality reduction.
# Needed by the UNI renderers and cohort projections. These were imported but
# undeclared until now, so a clean install produced a backend that booted and
# then failed at the first projection.
numpy>=1.24.0
h5py>=3.10.0
scikit-learn>=1.3.0
umap-learn>=0.5.5
# t-SNE for cohort projections (FFT-accelerated; sklearn's TSNE is too slow at cohort scale)
openTSNE>=1.0.0

# Clustering over cohort projections. k-means/HDBSCAN/agglomerative use
# scikit-learn (HDBSCAN needs >=1.3); Leiden needs these two.
igraph>=0.11.0
leidenalg>=0.10.0

# SSH for GPU cluster
paramiko>=3.4.0

# Form data / file uploads
python-multipart>=0.0.6

# Development
python-dotenv>=1.0.0


